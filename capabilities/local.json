{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "local",
  "description": "Local origins (the shell-owned status webview, tauri://localhost). Declaring the app ACL manifest enables the ACL check for local origins too, so every command the status page invokes must be granted here.",
  "windows": ["main"],
  "permissions": [
    "allow-daemon-info",
    "allow-set-overlay",
    "allow-plan-usage",
    "allow-set-scroll-freeze",
    "allow-toggle-devtools",
    "allow-update-info",
    "allow-open-url",
    "allow-notify",
    "allow-focus-window",
    "allow-toggle-maximize"
  ]
}
